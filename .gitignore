# Personal/runtime data — most of this now lives outside the repo tree
# entirely (see migrate_state_dir.py / FE-13), these entries are belt-and-
# suspenders for anything left behind by a pre-migration install or a
# skipped migration.
memory/
browser_screenshots/
projects/*/chats.json

# Claude Code intern context — local scratch, not project source
.claude/
CLAUDE_CONTEXT.md


# Python
__pycache__/
*.py[cod]
*.pyo
.env

# VS Code
.vscode/

# Logs
*.log
*.bk

# OS
.DS_Store
Thumbs.db

# Models (too large for git)
*.gguf
*.safetensors

# Eval harness scratch/output (MB-06)
eval/results/
eval/_scratch_data/
